---
title: AI 平台预算汇报
document_id: SOR-AI-TOKEN-2026-001-REPORT
version: V3.0
date: {{ generated_at }}
author: 业务发起部门
status: 评审中
---

# AI 平台预算汇报

## 1. 预算整体情况

> 基于内部调研数据，按实际使用费用 × 官方刊例价测算，**{{ PERSONS }} 人年度预算合计约 {{ "%.1f"|format(total_year_fee_wan + seat_budget.bundles.qoder_cn.seat_annual / 10000) }} 万元**，含以下两项采购：

| 采购项 | 年预算 | 说明 |
|:------|:------|:-----|
| Token 资源包 | **{{ "%.1f"|format(total_year_fee_wan) }} 万元** | 覆盖 6 个 AI 应用场景的模型调用费用 |
| 席位费 | **{{ "%.1f"|format(seat_budget.bundles.qoder_cn.seat_annual / 10000) }} 万元** | AI 编程助手及数字员工平台企业版席位（最低价方案） |
| **合计** | **{{ "%.1f"|format(total_year_fee_wan + seat_budget.bundles.qoder_cn.seat_annual / 10000) }} 万元** | — |

> 席位费按"全家桶"口径对比（AI 编程 + 数字员工一体），最低为 **Qoder CN 全家桶（阿里）199 元/席位/月**（含 VPC）。

## 2. 预算方法说明

### 2.1 Token 资源包预算

> **调研实际月费用 × 12 个月 × {{ "%.0f"|format((WAVE_FACTOR - 1) * 100) }}% 业务增长率 = 年度预算**

| 步骤 | 说明 |
|:----|:-----|
| ① 调研各团队实际月费用 | 覆盖 {{ survey_meta.product_lines }} 条产品线、{{ survey_meta.base_modules }} 个基础软件模块，62 条记录全部自洽 |
| ② 按官方刊例价反推 Token 量 | Token 量仅作规模参考，实际按金额签约 |
| ③ 乘以 {{ "%.0f"|format((WAVE_FACTOR - 1) * 100) }}% 业务增长率 | 覆盖业务增长和调试消耗，保守估计 |

> **为什么按金额签？** 主力模型价格随新模型推出会变化，各家计价方式不同（credits/积分/AFP），按 Token 量签不可控。

### 2.2 席位费预算

> **人数 × 席位单价 × 12 个月 = 年度席位费**

| 步骤 | 说明 |
|:----|:-----|
| ① 确定覆盖人数 | 与资源包预算一致，{{ PERSONS }} 人 |
| ② 全家桶口径比价 | 三家"全家桶"方案（AI 编程 + 数字员工一体）中最低为 **Qoder CN 全家桶 199 元/席位/月**（含 VPC） |
| ③ 赠送积分可折现 | 席位赠送的 Credits 按厂商资源包单价折为等值金额，从 Token 预算中扣减 |

> **全家桶口径说明**：Qoder CN 和 WorkBuddy 为一体化方案（AI 编程 + 数字员工），字节系需 **Trae（AI 编程）+ ArkClaw（数字员工）** 两个产品合并才能对标，合并后加权单价约 {{ "%.0f"|format(seat_budget.bundles.byte.price_per_seat_month) }} 元/席位/月。
> 各供应商方案对比详见《AI平台席位费预算测算说明》。

## 3. 核心数据

### 3.1 预算总览

| 指标 | 数值 | 说明 |
|:-----|:----:|:-----|
| 覆盖人数 | {{ PERSONS }} 人 | 调研覆盖 {{ survey_meta.survey_persons }} 人 + 扩展余量 |
| 月总费用（资源包） | {{ "{:,.0f}".format(total_month_fee) }} 元 | 各团队实际使用费用汇总 |
| 人均月费用（资源包） | **{{ "%.0f"|format(month_fee_per) }} 元/人/月** | 含 {{ "%.0f"|format((WAVE_FACTOR - 1) * 100) }}% 业务增长率后约 {{ "%.0f"|format(month_fee_per * WAVE_FACTOR) }} 元 |
| 年预算（资源包） | **{{ "%.1f"|format(total_year_fee_wan) }} 万元** | 月费用 × 12 × {{ WAVE_FACTOR }} |
| 年预算（席位费） | **{{ "%.1f"|format(seat_budget.bundles.qoder_cn.seat_annual / 10000) }} 万元** | Qoder CN 全家桶 VPC 版（最低价方案） |
| **年预算合计** | **{{ "%.1f"|format(total_year_fee_wan + seat_budget.bundles.qoder_cn.seat_annual / 10000) }} 万元** | 资源包 + 席位费 |

### 3.2 按模型分布

| 主力模型 | 占比 | 年预算(万元) | 说明 |
|:--------|:---:|:----------:|:-----|
{% for r in results -%}
| {{ r.model }} | {{ "%.1f"|format(r.pct) }}% | {{ "%.1f"|format(r.yf_wan) }} | {{ r.vendor }} {{ r.tier }}档 |
{% endfor %}
| **合计** | **100%** | **{{ "%.1f"|format(total_year_fee_wan) }}** | — |

> DeepSeek 占比 {{ "%.0f"|format(deepseek_fee / total_month_fee * 100) }}%，一家覆盖旗舰+轻量两档，便于打包议价。

### 3.3 按场景分布

| 场景 | 月费用(元) | 占比 | 说明 |
|:-----|:---------:|:---:|:-----|
{% for s in scenes -%}
| {{ s.name }} | {{ "{:,.0f}".format(s.fee) }} | {{ "%.1f"|format(s.pct) }}% | 人均{{ "%.0f"|format(s.per_person) }}元/月 |
{% endfor %}
| **合计** | **{{ "{:,.0f}".format(total_month_fee) }}** | 100% | — |

> 基础软件开发（人均 1,200 元/月）和代码生成（人均 278 元/月）是主要消耗场景，合计占比超 90%。

## 4. 采购建议

### 4.1 资源包采购

建议**按金额签框架协议，不按 Token 量签**：

1. 主力模型价格随新模型推出会变化，锁定 Token 量反而被动
2. 各家计价方式不同（credits/积分/AFP），按 Token 量签不可控
3. 框架协议可灵活调整模型配比，总量不超出年度上限即可

### 4.2 席位费采购

按"全家桶"口径对比（AI 编程 + 数字员工一体），**Qoder CN 全家桶（阿里）VPC 版（199 元/席位/月）** 为最低方案。

| 全家桶方案 | 组成 | 年席位费 | 席位单价 |
|:----------|:-----|:--------:|:--------:|
| **Qoder CN 全家桶（阿里）** | 一体化方案 | {{ "%.0f"|format(seat_budget.bundles.qoder_cn.seat_annual) }} 元 | **199 元/月** |
| 字节系全家桶（火山引擎） | Trae + ArkClaw | {{ "%.0f"|format(seat_budget.bundles.byte.seat_annual) }} 元 | {{ "%.0f"|format(seat_budget.bundles.byte.price_per_seat_month) }} 元/月 |
| WorkBuddy 全家桶（腾讯） | 一体化方案 | {{ "%.0f"|format(seat_budget.bundles.workbuddy.seat_annual) }} 元 | 316 元/月 |

> 字节系需 Trae（AI 编程）+ ArkClaw（数字员工）两个产品合并才能对标 Qoder CN 和 WorkBuddy 的一体化方案。
> 席位赠送的 Credits 可按厂商资源包单价折现，从 Token 预算中扣减。Qoder CN 企业资源包 80 元/2000 Credits（0.04 元/Credit），年赠送 Credits 折现约 {{ "%.0f"|format(seat_budget.vendors.qoder_cn.credit_value) }} 元。

## 5. 详细测算

完整的测算过程、公式推导、假设约束及数据校验，详见《AI平台资源包预算测算说明》和《AI平台席位费预算测算说明》。
