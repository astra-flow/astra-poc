---
title: AI 平台资源包预算测算说明
document_id: SOR-AI-TOKEN-2026-001
version: V4.0
date: {{ generated_at }}
author: 业务发起部门
status: 评审中
---

# 附件4_企业级AI平台资源包预算测算说明

## 1. 预算结论

| 采购项 | 金额 | 说明 |
|:------|:----:|:-----|
| Token 资源包年度预算 | **{{ "{:,.2f}".format(total_year_fee_yuan) }} 元** | 覆盖 {{ PERSONS }} 人 × 6 个 AI 应用场景 |
| 对应年 Token 需求量 | **{{ "{:,.0f}".format(rnd_year_token_M) }} 百万** | 按官方刊例价反推，取整 |
| 综合加权单价 | **{{ "%.2f"|format(rnd_overall_wp) }} 元/百万 Token** | 各模型按费用加权平均 |
| 采购方式 | 按金额签框架协议 | 不锁定模型配比，按年度总量控制 |

> 以上预算基于各团队 AI 使用情况调研实测数据测算，为预算上限，实际采购可通过商务谈判获得折扣。

## 2. 测算过程

### 2.1 第一步：调研各团队实际月费用

面向全院研发团队开展 AI 使用情况调研，收集各场景的模型使用费用数据：

| 项目 | 说明 |
|------|------|
| 数据源 | 《敏捷软件开发AI应用需求情况统计》调研表 |
| 覆盖范围 | {{ survey_meta.product_lines }} 条产品线 × 4 场景 + {{ survey_meta.base_modules }} 个基础软件模块 × 2 场景 |
| 有效记录 | {{ survey_meta.total_records }} 条，经校验全部自洽 |
| 覆盖人数 | 调研合计 {{ survey_meta.survey_persons }} 人，按 {{ PERSONS }} 人框算预算（含扩展余量） |

各模型族月费用汇总：

| 模型族 | 月费用（元） | 涉及场景 |
|--------|:----------:|---------|
{% for r in results -%}
| {{ r.model }} | {{ "{:,.2f}".format(r.fee) }} | {{ r.scenes }} |
{% endfor %}
| **合计** | **{{ "{:,.2f}".format(total_month_fee) }}** | — |

### 2.2 第二步：月费用 × 12 = 年度预算

$$
\text{年度预算} = \text{调研月总费用} \times 12 = {{ "{:,.0f}".format(total_month_fee) }} \times 12 = {{ "{:,.2f}".format(total_year_fee_yuan) }} \text{ 元}
$$

> 预算直接来自调研实测费用，不依赖任何假设或估算，数据可追溯。

### 2.3 第三步：按官方刊例价反推 Token 量

Token 量仅作为规模参考，用于采购申请填写和供应商询价。反推方法：用各模型的官方刊例价计算加权单价，再用费用 ÷ 单价得到 Token 量。

**加权单价计算公式：**

$$
P_{加权} = \frac{(P_{命中} \times {{ CACHE_HIT_RATE }} + P_{未命中} \times {{ 1 - CACHE_HIT_RATE }}) \times {{ INPUT_OUTPUT_RATIO[0] }} + P_{输出} \times {{ INPUT_OUTPUT_RATIO[1] }}}{4}
$$

> 其中输入:输出 = {{ INPUT_OUTPUT_RATIO[0] }}:{{ INPUT_OUTPUT_RATIO[1] }}，缓存命中率 = {{ CACHE_HIT_RATE * 100 }}%。参考 DeepSeek 官方推荐的代码生成场景典型值取中值设定。

**Token 量反推公式：**

$$
Q_{年}(\text{百万}) = \frac{F_{月} \times 12}{P_{加权}}
$$

**官方刊例价：**

| 模型族 | 厂商 | 输入(缓存命中) | 输入(缓存未命中) | 输出 | 加权单价 |
|--------|------|:--------------:|:---------------:|:----:|:--------:|
{% for p in official_prices_table -%}
| {{ p.model }} | {{ p.vendor }} | {{ p.input_hit }} | {{ p.input_miss }} | {{ p.output }} | {{ "%.4f"|format(p.wp) }} |
{% endfor %}

> 单价单位：元/百万 Token，来源：各厂商官方定价页（见第 6 节参考资料）

**反推结果：**

| 模型族 | 月费用（元） | 加权单价（元/百万） | 年Token(亿) | 年预算（元） |
|--------|:----------:|:-----------------:|:-----------:|:------------:|
{% for r in results -%}
| {{ r.model }} | {{ "{:,.2f}".format(r.fee) }} | {{ "%.4f"|format(r.wp) }} | {{ "{:,.0f}".format(r.yt_yi) }} | {{ "{:,.2f}".format(r.yf_yuan) }} |
{% endfor %}
| **合计** | **{{ "{:,.2f}".format(total_month_fee) }}** | — | **{{ "{:,.0f}".format(total_year_token_yi) }}** | **{{ "{:,.2f}".format(total_year_fee_yuan) }}** |

**资源包预算汇总（采购申请填表用）：**

| 字段 | 数值 |
|:-----|:----:|
| 年 Token 需求量 | **{{ "{:,.0f}".format(rnd_year_token_M) }} 百万** |
| 综合加权单价 | **{{ "%.2f"|format(rnd_overall_wp) }} 元/百万 Token** |
| 年预算金额 | **{{ "{:,.2f}".format(total_year_fee_yuan) }} 元** |
| 覆盖人数 | {{ PERSONS }} 人 |

## 3. 费用结构

### 3.1 按模型分布

| 供应商 | 模型族 | 年预算（元） | 占比 |
|--------|--------|:------------:|:----:|
| DeepSeek | V4 (Pro) + Flash | {{ "{:,.2f}".format(deepseek_fee * 12) }} | {{ "%.1f"|format(deepseek_fee / total_month_fee * 100) }}% |
| 智谱 | GLM-5.2 | {{ "{:,.2f}".format(glm_fee * 12) }} | {{ "%.1f"|format(glm_fee / total_month_fee * 100) }}% |
| 其他 | Codex + Gemini + Doubao | {{ "{:,.2f}".format(other_fee * 12) }} | {{ "%.1f"|format(other_fee / total_month_fee * 100) }}% |
| **合计** | — | **{{ "{:,.2f}".format(total_year_fee_yuan) }}** | 100% |

> DeepSeek 占比 {{ "%.0f"|format(deepseek_fee / total_month_fee * 100) }}%，一家覆盖旗舰+轻量两档，便于打包议价。

### 3.2 按场景分布

| 场景 | 月费用（元） | 使用人数 | 人均月费（元） | 占比 |
|------|:---------:|:-------:|:----------:|:---:|
{% for s in scenes -%}
| {{ s.name }} | {{ "{:,.2f}".format(s.fee) }} | {{ "%.0f"|format(s.persons) }} | {{ "%.2f"|format(s.per_person) }} | {{ "%.1f"|format(s.pct) }}% |
{% endfor %}
| **合计** | **{{ "{:,.2f}".format(total_month_fee) }}** | — | — | 100% |

> 基础软件开发和代码生成是主要消耗场景，合计占比超 90%。人均月费用 {{ "%.2f"|format(month_fee_per) }} 元，处于行业 AI 工具合理区间。

## 4. 采购方式建议

建议**按金额签框架协议，不按 Token 量签**：

1. 主力模型价格随新模型推出会变化，锁定 Token 量反而被动
2. 各家计价方式不同（credits/积分/AFP），按 Token 量签不可控
3. 框架协议可灵活调整模型配比，总量不超出年度上限即可

## 5. 假设与约束

1. **加权单价假设**：输入:输出 = {{ INPUT_OUTPUT_RATIO[0] }}:{{ INPUT_OUTPUT_RATIO[1] }}，缓存命中率 {{ CACHE_HIT_RATE * 100 }}%。参考 DeepSeek 官方推荐的代码生成场景典型值取中值设定。该假设仅影响 Token 量反推，不影响预算金额（预算来自实测费用）。

2. **覆盖人数**：按 {{ PERSONS }} 人测算（调研表合计 {{ survey_meta.survey_persons }} 人），含未覆盖人员的扩展余量。

3. **Token 量性质**：Token 量按官方刊例价反推，仅作规模参考。实际采购按金额签框架协议，不锁定模型配比。甲方有权根据业务需求调整模型使用比例，总量不超出年度上限即可。

## 6. 参考资料

| 序号 | 资料 | 链接 |
|:----:|------|------|
| 1 | DeepSeek 官方定价 | <https://api-docs.deepseek.com/zh-cn/quick_start/pricing\> |
| 2 | 智谱 GLM 定价 | <https://bigmodel.cn/pricing\> |
| 3 | 字节方舟模型价格 | <https://www.volcengine.com/docs/82379/1544106\> |
| 4 | 阿里百炼模型定价 | <https://www.alibabacloud.com/help/zh/model-studio/model-pricing\> |
| 5 | 腾讯 TokenHub | <https://cloud.tencent.com/product/tokenhub\> |

