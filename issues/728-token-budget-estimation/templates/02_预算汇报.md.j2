---
title: AI 平台预算汇报
document_id: SOR-AI-TOKEN-2026-001-REPORT
version: V3.0
date: {{ generated_at }}
author: 业务发起部门
status: 评审中
---

# AI 平台预算汇报

## 1. 预算整体情况

> 基于内部调研数据，按实际使用费用 × 官方刊例价测算，**{{ PERSONS }} 人年度预算合计约 {{ "{:,.2f}".format(total_year_fee_yuan + seat_budget.bundles.aliyun.seat_annual) }} 元**，含以下两项采购：

| 采购项 | 年预算 | 说明 |
|:------|:------|:-----|
| Token 资源包 | **{{ "{:,.2f}".format(total_year_fee_yuan) }} 元** | 覆盖 6 个 AI 应用场景的模型调用费用 |
| 席位费 | **{{ "{:,.2f}".format(seat_budget.bundles.aliyun.seat_annual) }} 元** | AI 编程助手及数字员工平台企业版席位（最低价方案） |
| **合计** | **{{ "{:,.2f}".format(total_year_fee_yuan + seat_budget.bundles.aliyun.seat_annual) }} 元** | — |

> 席位费按"全家桶"口径对比（AI 编程 + 数字员工一体），最低为 **Qoder CN 全家桶（阿里）199 元/席位/月**（含 VPC）。

## 2. 预算方法说明

### 2.1 Token 资源包预算

> **调研实际月费用 × 12 个月 = 年度预算**

| 步骤 | 说明 |
|:----|:-----|
| ① 调研各团队实际月费用 | 覆盖 {{ survey_meta.product_lines }} 条产品线、{{ survey_meta.base_modules }} 个基础软件模块，62 条记录全部自洽 |
| ② 按官方刊例价反推 Token 量 | Token 量仅作规模参考，实际按金额签约 |

> **为什么按金额签？** 主力模型价格随新模型推出会变化，各家计价方式不同（credits/积分/AFP），按 Token 量签不可控。

### 2.2 席位费预算

> **人数 × 席位单价 × 12 个月 = 年度席位费**

| 步骤 | 说明 |
|:----|:-----|
| ① 确定覆盖人数 | 与资源包预算一致，{{ PERSONS }} 人 |
| ② 全家桶口径比价 | 三家"全家桶"方案（AI 编程 + 数字员工一体）中最低为 **Qoder CN 全家桶 199 元/席位/月**（含 VPC） |
| ③ 赠送积分可折现 | 席位赠送的 Credits 按厂商资源包单价折为等值金额，从 Token 预算中扣减 |

> **全家桶口径说明**：Qoder CN 和 WorkBuddy 为一体化方案（AI 编程 + 数字员工），字节系需 **Trae（AI 编程）+ ArkClaw（数字员工）** 两个产品合并才能对标，合并后加权单价约 {{ "%.2f"|format(seat_budget.bundles.volcengine.price_per_seat_month) }} 元/席位/月。
> 各供应商方案对比详见《AI平台席位费预算测算说明》。

## 3. 核心数据

### 3.1 预算总览

| 指标 | 数值 | 说明 |
|:-----|:----:|:-----|
| 覆盖人数 | {{ PERSONS }} 人 | 调研覆盖 {{ survey_meta.survey_persons }} 人 + 扩展余量 |
| 月总费用（资源包） | {{ "{:,.2f}".format(total_month_fee) }} 元 | 各团队实际使用费用汇总 |
| 人均月费用（资源包） | **{{ "%.2f"|format(month_fee_per) }} 元/人/月** |
| 年预算（资源包） | **{{ "{:,.2f}".format(total_year_fee_yuan) }} 元** | 月费用 × 12 |
| 年预算（席位费） | **{{ "{:,.2f}".format(seat_budget.bundles.aliyun.seat_annual) }} 元** | Qoder CN 全家桶 VPC 版（最低价方案） |
| **年预算合计** | **{{ "{:,.2f}".format(total_year_fee_yuan + seat_budget.bundles.aliyun.seat_annual) }} 元** | 资源包 + 席位费 |

### 3.2 资源包预算说明

#### 3.2.1 资源包预算概览

| 指标 | 数值 |
|:-----|:----:|
| 年 Token 需求量 | **{{ "{:,.0f}".format(total_year_token_yi) }} 亿** |
| 综合加权单价 | **{{ "%.2f"|format(overall_wp) }} 元/百万 Token** |
| 年预算金额 | **{{ "{:,.2f}".format(total_year_fee_yuan) }} 元** |

> **加权单价说明**：由于不同模型（如 DeepSeek V4、GLM 5.2 等）的定价不同，我们按各模型的实际使用费用占比加权计算出一个"平均单价"——称为**综合加权单价**。简单来说，就是把所有模型的调用费用总和，除以所有模型的 Token 消耗总量，得到一个折合每百万 Token 的综合价格。这个价格可以直接用于采购申请中的单价填写。

> 例如：假设团队用了两个模型，一个便宜的 1 元/百万 Token（用量大），一个贵的 5 元/百万 Token（用量小），综合加权单价会介于两者之间，偏向用量大的那个。按此方法测算，本项目的综合加权单价为 **{{ "%.2f"|format(overall_wp) }} 元/百万 Token**。

#### 3.2.2 按模型分布

| 主力模型 | 占比 | 年预算(元) | 说明 |
|:--------|:---:|:----------:|:-----|
{% for r in results -%}
| {{ r.model }} | {{ "%.1f"|format(r.pct) }}% | {{ "{:,.2f}".format(r.yf_yuan) }} | {{ r.vendor }} {{ r.tier }}档 |
{% endfor %}
| **合计** | **100%** | **{{ "{:,.2f}".format(total_year_fee_yuan) }}** | — |

> DeepSeek 占比 {{ "%.0f"|format(deepseek_fee / total_month_fee * 100) }}%，一家覆盖旗舰+轻量两档，便于打包议价。

### 3.2.3 按场景分布

| 场景 | 月费用(元) | 占比 | 说明 |
|:-----|:---------:|:---:|:-----|
{% for s in scenes -%}
| {{ s.name }} | {{ "{:,.2f}".format(s.fee) }} | {{ "%.1f"|format(s.pct) }}% | 人均{{ "%.2f"|format(s.per_person) }}元/月 |
{% endfor %}
| **合计** | **{{ "{:,.2f}".format(total_month_fee) }}** | 100% | — |

> 基础软件开发（人均 1,200 元/月）和代码生成（人均 278 元/月）是主要消耗场景，合计占比超 90%。

## 4. 采购建议

### 4.1 资源包采购

建议**按金额签框架协议，不按 Token 量签**：

1. 主力模型价格随新模型推出会变化，锁定 Token 量反而被动
2. 各家计价方式不同（credits/积分/AFP），按 Token 量签不可控
3. 框架协议可灵活调整模型配比，总量不超出年度上限即可

### 4.2 席位费采购

按"全家桶"口径对比（AI 编程 + 数字员工一体），**Qoder CN 全家桶（阿里）VPC 版（199 元/席位/月）** 为最低方案。

| 全家桶方案 | 组成 | 年席位费 | 席位单价 |
|:----------|:-----|:--------:|:--------:|
| **Qoder CN 全家桶（阿里）** | 一体化方案 | {{ "%.2f"|format(seat_budget.bundles.aliyun.seat_annual) }} 元 | **199 元/月** |
| 字节系全家桶（火山引擎） | Trae + ArkClaw | {{ "%.2f"|format(seat_budget.bundles.volcengine.seat_annual) }} 元 | {{ "%.2f"|format(seat_budget.bundles.volcengine.price_per_seat_month) }} 元/月 |
| WorkBuddy 全家桶（腾讯） | 一体化方案 | {{ "%.2f"|format(seat_budget.bundles.tencent.seat_annual) }} 元 | 316 元/月 |

> 字节系需 Trae（AI 编程）+ ArkClaw（数字员工）两个产品合并才能对标 Qoder CN 和 WorkBuddy 的一体化方案。
> 席位赠送的 Credits 可按厂商资源包单价折现，从 Token 预算中扣减。Qoder CN 企业资源包 80 元/2000 Credits（0.04 元/Credit），年赠送 Credits 折现约 {{ "%.2f"|format(seat_budget.vendors["阿里云 Qoder CN"].credit_value) }} 元。

## 5. 详细测算

完整的测算过程、公式推导、假设约束及数据校验，详见《AI平台资源包预算测算说明》和《AI平台席位费预算测算说明》。
