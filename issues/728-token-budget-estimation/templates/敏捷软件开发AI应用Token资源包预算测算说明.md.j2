---
title: 敏捷软件开发 AI 应用 Token 资源包预算测算说明
document_id: SOR-AI-TOKEN-2026-001
version: V2.2
date: 2026-07-03
author: 业务发起部门
status: 评审中
---

# 敏捷软件开发 AI 应用 Token 资源包预算测算说明

## 1. 文档目的

本文档为 AI 大模型 Token 资源包采购申请的费用测算附件，说明预算金额的测算依据、数据来源与计算逻辑，确保预算可追溯、可审计、逻辑自洽。

## 2. 采购背景

为支撑敏捷软件开发全流程的 AI 赋能，研发团队已在 PRD 生成、架构图生成、代码生成、自动化测试脚本、基础软件开发、日志分析等 6 类场景中广泛使用 AI 大模型。本次采购旨在通过预付费 Token 资源包或框架协议方式统一保障各场景的模型调用额度。

## 3. 数据来源

### 3.1 调研数据

| 项目 | 说明 |
|------|------|
| 数据源 | 《敏捷软件开发AI应用需求情况统计》调研表 |
| 覆盖范围 | {{ survey_meta.product_lines }} 条产品线 × 4 场景（应用产品）+ {{ survey_meta.base_modules }} 个基础软件模块 × 2 场景（基础软件）|
| 有效记录 | 应用产品 {{ survey_meta.app_records }} 条 + 基础软件 {{ survey_meta.base_records }} 条 = {{ survey_meta.total_records }} 条 |
| 统计维度 | 功能模块信息、AI 模型、Token 消耗量、费用情况、使用人数、月总费用 |

### 3.2 官方刊例价数据

| 模型族 | 输入(缓存命中) | 输入(缓存未命中) | 输出 | 加权单价 |
|--------|:--------------:|:---------------:|:----:|:--------:|
{% for p in official_prices_table -%}
| {{ p.model }} | {{ p.input_hit }} | {{ p.input_miss }} | {{ p.output }} | {{ "%.4f"|format(p.wp) }} |
{% endfor %}

> 单价单位：元/百万 Token
> 单价来源：DeepSeek 官方 <https://api-docs.deepseek.com/zh-cn/quick_start/pricing>
> 加权假设：输入:输出 = {{ INPUT_OUTPUT_RATIO[0] }}:{{ INPUT_OUTPUT_RATIO[1] }}，缓存命中率 = {{ CACHE_HIT_RATE * 100 }}%
> GLM-5.2 智谱官方定价（输入 8/输出 28/缓存命中 2 元/百万Token）；Codex/Gemini 按 Pro 档 ×1.2 折算（订阅制/国际模型溢价）；Doubao-Seed-Code 按 Flash 同档。
> 原调研中 Qwen3-Plus 已折算到 DeepSeek-V4-Flash（同类均衡型模型，便于供应商集中议价）。

## 4. 测算方法

### 4.1 方法论选择

调研表包含两类数据，可信度不同：

| 数据类型 | 可信度 | 依据 |
|---------|:------:|------|
| 费用数据（人均月费用 × 使用人数 = 月总费用） | 高 | 62 条应用产品记录经校验全部自洽，0 条偏差 |
| Token 消耗量数据 | 低 | 反推单价远低于官方刊例价 16-108 倍，存在量纲错误或填报不规范 |

基于上述校验结果，本次测算采用**费用主线法**：以可信的费用数据为基准，结合官方刊例价反推 Token 消耗量，作为规模参考。

### 4.2 计算公式

**加权单价**（假设输入:输出 = {{ INPUT_OUTPUT_RATIO[0] }}:{{ INPUT_OUTPUT_RATIO[1] }}，缓存命中率 = {{ CACHE_HIT_RATE * 100 }}%）：

$$
P_{加权} = \frac{(P_{命中} \times {{ CACHE_HIT_RATE }} + P_{未命中} \times {{ 1 - CACHE_HIT_RATE }}) \times {{ INPUT_OUTPUT_RATIO[0] }} + P_{输出} \times {{ INPUT_OUTPUT_RATIO[1] }}}{% if INPUT_OUTPUT_RATIO[0] + INPUT_OUTPUT_RATIO[1] != 4 %}{{ INPUT_OUTPUT_RATIO[0] + INPUT_OUTPUT_RATIO[1] }}{% else %}4{% endif %}
$$

**月 Token 量**：

$$
Q_{月}(\text{百万}) = \frac{F_{月}}{P_{加权}}
$$

**年度预算**：

$$
B_{年} = F_{月} \times 12
$$

**单位换算**（已验证）：

- 1 百万 = 1,000,000 个 Token
- 1 亿 = 100,000,000 个 Token = **100 百万**
- 百万 Token ÷ 100 = 亿 Token

### 4.3 采购方式建议

建议按金额签框架协议，不按 Token 量签，理由：

1. **主力模型价格浮动**：随新模型推出会变化
2. **新模型价格未知**：新模型我们肯定要用，但价格无法预判
3. **各家计价不一**：各家 Token 计价方式不同，且资源包有折扣，按 Token 量签不可控

## 5. 测算结果

### 5.1 按模型族聚合的月度费用

| 模型族 | 月费用（元） | 记录数 | 涉及场景 |
|--------|:----------:|:------:|---------|
{% for r in results -%}
| {{ r.model }} | {{ "{:,.2f}".format(r.fee) }} | {{ r.records }} | {{ r.scenes }} |
{% endfor %}
| **合计** | **{{ "{:,.2f}".format(total_month_fee) }}** | **187** | — |

### 5.2 Token 量反推与年度预算

| 模型族 | 月费用（元） | 加权单价（元/百万） | 月Token(百万) | 月Token(亿) | 年Token(亿) | 年预算（元） |
|--------|:----------:|:-----------------:|:-------------:|:-----------:|:-----------:|:------------:|
{% for r in results -%}
| {{ r.model }} | {{ "%.2f"|format(r.fee) }} | {{ "%.4f"|format(r.wp) }} | {{ "%.0f"|format(r.mt_M) }} | {{ "%.2f"|format(r.mt_yi) }} | {{ "%.0f"|format(r.yt_yi) }} | {{ "{:,.2f}".format(r.yf_yuan) }} |
{% endfor %}
| **合计** | **{{ "{:,.2f}".format(total_month_fee) }}** | — | — | — | **{{ "{:,.0f}".format(total_year_token_yi) }}** | **{{ "{:,.2f}".format(total_year_fee_yuan) }}** |

> Token 量按官方刊例价反推，作为规模参考；实际采购按金额签框架协议。

#### 资源包预算汇总

{% set rnd_token = (total_year_token_yi * 100 // 1000) * 1000 %}
{% set rnd_wp = total_year_fee_yuan / rnd_token %}
| 字段 | 数值 | 说明 |
|:-----|:----:|:-----|
| 年预算金额 | **{{ "{:,.2f}".format(total_year_fee_yuan) }} 元** | 按金额签框架协议 |
| 年 Token 需求量（参考） | **{{ "{:,.0f}".format(rnd_token) }} 百万** | 按刊例价反推，约 {{ "%.2f"|format(rnd_wp) }} 元/百万 Token |
| 覆盖人数 | {{ PERSONS }} 人 | 调研覆盖 + 扩展余量 |

### 5.3 人均口径（按 {{ PERSONS }} 人）

| 指标 | 金额 |
|------|:----:|
| 月总费用 | {{ "{:,.2f}".format(total_month_fee) }} 元 |
| 人均月费用 | **{{ "%.2f"|format(month_fee_per) }} 元/人/月** |
| 人均月 Token | {{ "%.0f"|format(month_token_per_M) }} 百万 = **{{ "%.2f"|format(month_token_per_yi) }} 亿/人/月** |
| 人均年 Token | {{ "%.2f"|format(year_token_per_yi) }} 亿/人/年 |
| 人均年预算 | {{ "{:,.2f}".format(year_fee_per) }} 元/人/年 |
| {{ PERSONS }} 人年预算合计 | **{{ "{:,.2f}".format(total_year_fee_yuan) }} 元** |
| {{ PERSONS }} 人年 Token 合计 | **{{ "{:,.0f}".format(total_year_token_yi) }} 亿** |

人均费用呈"两极分布"特征：

- 基础软件开发团队因采用三模型并行开发，人均强度较高（1,200 元/月）
- 应用产品团队以代码生成和 PRD 生成为主，人均 48-700 元/月
- 整体人均 {{ "%.2f"|format(month_fee_per) }} 元/月，处于行业 AI 工具人均成本的合理区间

## 6. 费用结构分析

### 6.1 按场景维度

| 场景 | 月费用（元） | 使用人数 | 人均月费（元） | 占比 |
|------|:---------:|:-------:|:----------:|:---:|
{% for s in scenes -%}
| {{ s.name }} | {{ "%.2f"|format(s.fee) }} | {{ "%.0f"|format(s.persons) }} | {{ "%.2f"|format(s.per_person) }} | {{ "%.1f"|format(s.pct) }}% |
{% endfor %}
| **合计** | **{{ "{:,.2f}".format(total_month_fee) }}** | — | — | 100% |

### 6.2 供应商集中度

| 供应商 | 模型族 | 年预算（元） | 占比 |
|--------|--------|:------------:|:----:|
| DeepSeek | V4 (Pro) + Flash | {{ "{:,.2f}".format(deepseek_fee * 12) }} | {{ "%.1f"|format(deepseek_fee / total_month_fee * 100) }}% |
| 智谱 | GLM-5.2 | {{ "{:,.2f}".format(glm_fee * 12) }} | {{ "%.1f"|format(glm_fee / total_month_fee * 100) }}% |
| 其他 | Codex + Gemini + Doubao | {{ "{:,.2f}".format(other_fee * 12) }} | {{ "%.1f"|format(other_fee / total_month_fee * 100) }}% |
| **合计** | — | **{{ "{:,.2f}".format(total_year_fee_yuan) }}** | 100% |

> DeepSeek 一家覆盖旗舰 + 轻量两档，供应商集中度高，便于打包议价。

### 6.3 模型配比（测算依据，合同不锁定）

各模型 Token 消耗量占比如下，该比例用于预算测算及供应商询价，实际采购采用框架协议模式，不锁定具体模型配比，按年度总量控制：

| 模型族 | 年Token(亿) | 占比 |
|--------|:-----------------:|:----:|
{% for r in results -%}
| {{ r.model }} | {{ "%.0f"|format(r.yt_buf) }} | {{ "%.1f"|format(r.pct) }}% |
{% endfor %}
| **合计** | **{{ "{:,.0f}".format(total_year_token_yi) }}** | 100% |

## 7. 模型名称归一化说明

调研表中原始模型名称存在拼写错误与多种写法，已统一归一到 5 个采购模型族。其中原 Qwen3-Plus 用量折算到 DeepSeek-V4-Flash：

| 归一化模型族 | 原始写法示例 | 处理说明 |
|-------------|------------|---------|
| DeepSeek-V4 | deepseek-v4-pro、deepseekv4 pro、deepseekv4、DeepSeek | 大小写统一，保留 Pro 表示旗舰档 |
| DeepSeek-V4-Flash | Qwen、Qwen3 系列、Qwen3.7plus、千问（原 Qwen3-Plus 折算） | 原 Qwen3-Plus 为阿里均衡型模型，折算到 DeepSeek-V4-Flash（同类均衡型） |
| GLM-5.2 | GLM5.x、GML 5.1（拼写错误）、GLM5.22 | 修正 GML→GLM，统一到 5.2 版本 |
| Codex | Codex | OpenAI Codex，保持 |
| Doubao-Seed-Code | Duubao-Seed-Code（拼写错误）、Doubao-Seed-2.0-Code | 修正 Duubao→Doubao |
| Gemini | Gemini | 保持 |

> 折算理由：Qwen3-Plus 并非不可替代模型，折算后供应商集中度提升，议价空间更大。

## 8. 假设与约束

1. **输入输出比假设**：加权单价按输入:输出 = {{ INPUT_OUTPUT_RATIO[0] }}:{{ INPUT_OUTPUT_RATIO[1] }} 计算，缓存命中率 {{ CACHE_HIT_RATE * 100 }}%，基于典型业务场景。实际比例可能因场景而异，但对费用主线测算无影响（费用数据为调研实测值）。

2. **GLM-5.2 单价**：智谱官方定价（输入 8/输出 28/缓存命中 2 元/百万Token），加权单价 10.75 元/百万Token。

3. **覆盖人数**：按 {{ PERSONS }} 人测算（应用产品各场景合计 {{ survey_meta.survey_persons }} 人 + 基础软件 127 人 + 其他业务 30 人），调研表实际覆盖 {{ survey_meta.survey_persons }} 人，{{ PERSONS }} 人含未覆盖人员的扩展余量。

4. **费用口径**：PRD 生成场景费用仅含纯 API 调用费，ArkClaw 席位费和 Coding Plan 费用已剔除，走单独采购流程。

5. **Qwen3-Plus 折算**：原调研表中 Qwen3-Plus 用量（月费用 65,067 元）已折算到 DeepSeek-V4-Flash。折算后费用金额不变，但 Token 量按 Flash 单价反推（Flash 单价更低，Token 量更大）。

6. **Token 量性质**：本文档 Token 量均按官方刊例价反推，作为规模参考。实际采购建议按金额签框架协议，不按 Token 量签（理由见 4.3 节）。

7. **模型配比不锁定**：第 6.3 节模型配比仅用于预算测算及供应商询价，实际采购采用框架协议模式，不锁定具体模型配比。甲方有权根据实际业务需求调整模型使用比例，总量不超出年度上限即可。

## 9. 修订历史

| 版本 | 日期 | 修订内容 | 修订人 |
|------|------|---------|--------|
| V1.0 | 2026-07-03 | 初版发布（阿里云转售价，6 个模型族）| 业务发起部门 |
| V1.1 | 2026-07-03 | PRD 生成费用剔除 ArkClaw 席位费和 Coding Plan | 业务发起部门 |
| V1.2 | 2026-07-03 | Qwen3-Plus 折算到 DeepSeek-V4-Flash | 业务发起部门 |
| V1.3 | 2026-07-03 | 修正 Token 量单位错误（万→亿）| 业务发起部门 |
| V2.0 | 2026-07-03 | 改用 DeepSeek 官方刊例价（非阿里云转售价）| 业务发起部门 |
| V2.1 | 2026-07-03 | 修正百万→亿换算系数（÷100 而非 ÷10000）；所有数字以脚本 00_official_calc.py 输出为准 | 业务发起部门 |
| V2.2 | 2026-07-03 | 新增6.3节模型配比（测算依据，合同不锁定）；新增假设8模型配比不锁定条款 | 业务发起部门 |
